# 원본 기록 이관 인벤토리

## 원본 보존 원칙

아래 OneDrive 경로는 원본 보관소다. 이 저장소로 이동·삭제·일괄 복사하지 않는다.

| 원본 | 규모 | 이관 방향 |
| --- | ---: | --- |
| `Desktop\\실습 관련` | 12개 | SQL·DB 학습 내용을 일반화한 Markdown으로 이관 |
| `Desktop\\업무 관련` | 약 29,678개 | 주제별 기록만 선별 이관, 소스·산출물·원문은 제외 |
| `Desktop\\-` | 49개 | 참고자료만 검토, 인사·세무·연락망·휴가 자료는 제외 |

## 1차 이관 완료

- SQL 기본 DML 구조
- PL/SQL 블록 및 채번 시 고려사항
- SQL 조인·집계·검증 체크리스트
- 화면 기능 구현 흐름
- Vue 대시보드 개인화와 반응형 데이터 흐름
- 메뉴 구성과 접근 표시 검증
- 일일 이관 체크리스트와 우선순위 백로그
- 리포트 변경과 검증 흐름
- 모바일 API 변경과 검증 흐름
- 서브쿼리 별칭과 존재 조건 점검
- 변경 되돌리기와 복구 작업 흐름
- 통합 결과 집합의 페이지 나누기

일상 이관 절차는 [일일 기록 이관 체크리스트](daily-checklist.md), 현재 작업 순서는 [기록 이관 백로그](backlog.md)에서 관리한다.

## 다음 선별 우선순위

1. `업무 관련/리포트`: 텍스트 원본 2개를 검토하여 변경·검증 기준을 일반화해 반입했다. 실제 리포트, 소스·쿼리, 배포·접속 식별값은 제외했다.
2. `업무 관련/모바일 관련`: 텍스트 원본 3개를 검토하여 API 변경·검증 흐름을 일반화해 반입했다. 실제 저장소·브랜치·접속 주소·배포 설정·API 경로·소스·쿼리는 제외했다.
3. `Desktop\\-\\참고자료`: SQL 관련 프레젠테이션 3개와 버전관리 프레젠테이션 1개를 검토했다. SQL의 별칭·존재 조건, 통합 결과의 전역 순번·페이지 검증, Git의 파일 복원·공유 커밋 상쇄·검증 흐름을 반입했다. 메뉴 등록 텍스트 메모와 사용자·메뉴 프레젠테이션은 기존 메뉴 노트와 중복되어 새 문서를 만들지 않았다. 개인·회사·현장 식별값이 포함된 협력사·현장 자료와 내부 스키마·운영 식별값이 포함된 실제 SQL 전문은 제외했다.
4. `업무 관련/권한별 메뉴추가`: 텍스트 원본 3개를 검토해, 요청 범위 확정, 조직·그룹·개별 사용자 권한의 적용 순서, 다수 대상의 일괄 적용과 역할별 검증을 일반화해 반입했다. 고객·협력사·역할·사용자 식별값, 운영 서버·화면 경로, 실제 메뉴 목록과 채팅 원문은 제외했다.
5. 다음 자동 실행 전: 상태가 정확히 `대기`인 배치를 지정해야 한다. P2 `유지보수·요청 대응`의 계속 처리 또는 P2 `플랫폼별 업무 기록`의 화면설계·테이블·쿼리 선별이 후보다.

## 제외 또는 별도 검토

- 연락망, 인사·세무·휴가·총무, 메일·회의·메신저 원문
- DB 터널링, 서버·보안·배포 설정, 라이선스 갱신 자료
- 고객 제공 파일과 첨부파일, 운영 로그, 원본 SQL·ERD·소스·빌드 산출물
